import React from "react";

const Hero = () => {
  return (
    <section className="hero">
      <h2 className="main-title">Eyewear for every side of you</h2>
    </section>
  );
};

export default Hero;
